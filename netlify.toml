[build]
  command = "npm run build"
  publish = "dist"

# SPA – alla routes serverar index.html (statiska filer i /assets/ serveras först)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
